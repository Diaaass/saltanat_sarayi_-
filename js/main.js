/* =========================================================================
   Saltanat Saraiy — main.js
   Скелет. Поведение навешивается через data-* атрибуты (см. бриф п.1.5).
   ========================================================================= */

(() => {
  'use strict';

  // ---------------------------------------------------------------------
  // 1. Модалка заявки
  //    data-modal-target="modal-feedback" — открыть
  //    data-modal-close — закрыть (крестик / клик по оверлею)
  //    Esc — закрыть; ловушка фокуса; .is-lock на <body>
  // ---------------------------------------------------------------------
  // TODO


  // ---------------------------------------------------------------------
  // 2. Бургер-меню
  //    data-toggle-target="[data-header]" + data-toggle-classname="menu-active"
  // ---------------------------------------------------------------------
  // TODO


  // ---------------------------------------------------------------------
  // 3. Аккордеоны
  //    Нативный <details>/<summary> — JS не нужен. При необходимости —
  //    плавная анимация высоты.
  // ---------------------------------------------------------------------
  // TODO (если потребуется анимация раскрытия)


  // ---------------------------------------------------------------------
  // 4. Формы (модалка + контакты)
  //    Сабмит → переход на thx.html. Точка интеграции с бэком — позже.
  // ---------------------------------------------------------------------
  // TODO


  // ---------------------------------------------------------------------
  // 5. Lenis (smooth scroll) + Swiper (галерея / карточки на мобайле)
  //    Подключаются опционально, инициализируются при наличии узлов.
  // ---------------------------------------------------------------------
  // TODO


  // ---------------------------------------------------------------------
  // 6. Площадки — scroll-свитчер (GSAP + ScrollTrigger, см. бриф п.10.1)
  //    Pin + snap + кроссфейд фона. На мобайле и при prefers-reduced-motion
  //    не инициализировать тяжёлый моушен.
  // ---------------------------------------------------------------------
  // TODO

})();
